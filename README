Sabari provides directory listing, displaying images as thumbnails, and files as type-icons. It supports custom themes and image tags(python version only. Not available yet).

Setup:
python imagemanager.py setup
